<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Doner Place</title>
    <link href="/styles/style.css" type="text/css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <header>
            <span class="logo">Doner  Place</span>
            {{#if username}}
                <span class="greeting">Welcome, {{username}} | <a href="/user/logout">[logout]</a></span>
            {{/if}}
            <ul class="menu">
				<li><a class="button" href="/">Home</a></li>
             {{#if username}}  
                <li><a class="button" href="/order/status">Order Status</a></li>
             {{else}}
                <li><a class="button" href="/user/login">Login</a></li>
                <li><a class="button" href="/user/register">Register</a></li>
             {{/if}}
            </ul>
        </header>
        <main>
            {{#if error }}
            <center>  <h2 style="color: red">{{error}}</h2></center>
            {{else if success}}
            <center>  <h2  style="color: green">{{success}}</h2></center>
            {{/if}}
           {{{body}}}
        </main>
        <footer>Doner Place &copy; 2017</footer>
    </div>
</body>
</html>