<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Wiki</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>

<body>
    <div>
        <aside>
            <div class="center">
                <img class="logo" src="/public/images/logo.png" alt="logo">
            </div>

            <nav>
                <a href="/">Main Page</a>
                <a href="/latest-article">Latest Article</a>
                <a href="/all-articles">All Articles</a>
                <form>
                    <input type="text">
                    <input type="submit" value="Search">
                </form>
                 {{#if data.email}}
                <a href="/create">Create New Article</a>
                 {{/if}}
            </nav>
        </aside>
        <div id="container">
            <header>
                {{#if data.email}}
                {{else}}
                <a href="/user/login">Login</a>
                <a href="/user/register">Register</a>
                {{/if}}
                 {{#if data.email}}
                <span>Welcome, {{data.email}}!</span>
                <a href="/user/logout">[Logout]</a>
                {{/if}}
            </header>
            <main>
                 {{#if data.error }}
                <center>  <h2 style="color: red">{{data.error}}</h2></center>
                {{else if data.success}}
                <center>  <h2  style="color: green">{{data.success}}</h2></center>
                {{/if}}
                {{{body}}}
            </main>
            <footer>
                SoftUni Wiki &copy; 2018
            </footer>
        </div>
    </div>
</body>
</html>
